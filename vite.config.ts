// Vite config — uses @lovable.dev/vite-tanstack-config which bundles all required plugins.
// Do NOT add tanstackStart, viteReact, tailwindcss, tsConfigPaths, or cloudflare manually.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
